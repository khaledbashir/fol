#!/usr/bin/env sh

# Run zero bullshit check first (only on staged files)
echo "🔍 Running ZERO BULLSHIT check..."
node .husky/zero-bullshit-check.js $(git diff --cached --name-only --diff-filter=ACM)

# Run lint-staged
echo "🔧 Running lint-staged..."
npx lint-staged